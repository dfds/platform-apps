apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::${flux_workload_account_id}:role/grafana-${flux_cluster_name}-irsa
    eks.amazonaws.com/sts-regional-endpoints: "true"
  name: grafana
  namespace: grafana