apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - sources.yaml
  - release.yaml
patches:
  - path: ${flux_velero_patch_file}
    target:
      group: helm.toolkit.fluxcd.io
      kind: HelmRelease
      name: velero
      namespace: velero
